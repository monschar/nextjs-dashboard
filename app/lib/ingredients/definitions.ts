export type Invoice = {
  id: string;
  name: string;
  date: string;
  price: number;
  tag: string;
  image_url: string;
  stock: number;
};

export type IngredientsTable = {
  id: string;
  name: string;
  price: number;
  date: string;
  tag: string;
  image_url: string;
  stock: number;
};

export type IngredientForm = {
  id: string;
  name: string;
  price: number;
  tag: string;
  image_url: string;
  stock: number;
};
